# 全年度行事曆 (Yearly Planner)

一個使用 React + Vite + Tailwind CSS 打造的極簡年度行事曆，資料儲存於瀏覽器 localStorage。

## 功能

- 📅 一眼看清全年 365 天
- 📌 12 個月以卡片網格呈現（響應式：手機 1 欄、平板 2 欄、桌面 3–4 欄）
- 🔴 有事件的日期顯示紅色圓點與事件數量
- 📝 點擊日期可新增、編輯、刪除事件（含時間與標題）
- 💾 資料自動儲存於 localStorage，重新整理後仍存在

## 技術堆疊

- React 18 + Vite
- Tailwind CSS
- lucide-react 圖示

## 安裝與執行

```bash
cd yearly-planner
npm install
npm run dev
```

接著在瀏覽器開啟 `http://localhost:5173` 即可使用。

## 專案結構

```
yearly-planner/
├── src/
│   ├── components/
│   │   ├── DateCell.jsx     # 單一日期的格子
│   │   ├── EventItem.jsx    # 事件列（含編輯、刪除）
│   │   ├── EventModal.jsx   # 日期點擊後的彈出視窗
│   │   ├── MonthCard.jsx    # 單一月份日曆卡片
│   │   └── YearlyCalendar.jsx  # 主畫面
│   ├── hooks/
│   │   └── useEvents.js     # 事件管理 + localStorage
│   ├── types/
│   │   └── event.js         # 型別與工具函式
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
├── package.json
└── README.md
```
